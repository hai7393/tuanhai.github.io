import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container : {
        marginLeft : 25
    },
    containerIconHeaderRight:{
        flexDirection:'row',
        marginRight:12,
    }
})

export default styles;